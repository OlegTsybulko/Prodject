import React from "react";
import styled from "styled-components";

const NotFaundBlok: React.FC = () => {
  return <Title> Не чего не найдено (</Title>;
};

const Title = styled.h1`
  color: black;
  text-align: center;
`;

export default NotFaundBlok;
